Palindrome game
==================================
Getting Started
---------------

```sh
cd palindrome

# Build the docker
docker build -t palindrome.

# run the docker
docker run -p 3000:3000 palindrome
 

```
